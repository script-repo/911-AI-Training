# Database Configuration
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/911_training

# Database Pool Settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# SQL Logging (set to 'true' to see SQL queries)
SQL_ECHO=false

# Application Settings
APP_ENV=development
DEBUG=true
